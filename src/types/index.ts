import { Node, Edge } from '@xyflow/react';export type NodeStatus = 'Success' | 'Error' | 'Warning';export type MetricTab = 'CPU' | 'Memory' | 'Disk';export interface AppNodeData extends Record<string, unknown> {  label: string;  type: 'Database' | 'Service' | 'Cache';  status: NodeStatus;  traffic: number;   cost: string;  region: string;  specs: {    cpu: string;    memory: string;    disk: string;  };}export type AppNode = Node<AppNodeData>;export interface GraphData {  nodes: AppNode[];  edges: Edge[];}export interface AppData {  id: string;  name: string;  iconBg: string;}