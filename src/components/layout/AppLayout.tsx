import { ReactNode } from "react";import { useAppStore } from "../../lib/store";import { Sheet, SheetContent } from "@/components/ui/sheet";import { Boxes, LayoutGrid, Settings, Database } from "lucide-react";export function AppLayout({ children, rightPanel }: { children: ReactNode; rightPanel: ReactNode }) {  const { isMobilePanelOpen, setMobilePanelOpen } = useAppStore();  return (    <div className="flex h-screen w-full flex-col bg-background text-foreground overflow-hidden">      <header className="flex h-14 items-center border-b px-4 lg:h-[60px]">        <div className="flex items-center gap-2 font-semibold">          <Boxes className="h-5 w-5" />          <span>Ainyx Builder</span>        </div>      </header>      <div className="flex flex-1 overflow-hidden">        <aside className="hidden w-[60px] flex-col items-center border-r py-4 md:flex bg-muted/30">          <div className="grid gap-4 pt-4">             <LayoutGrid className="h-6 w-6 text-primary" />             <Database className="h-6 w-6 text-muted-foreground" />             <Settings className="h-6 w-6 text-muted-foreground" />          </div>        </aside>        <main className="flex-1 relative">{children}</main>        <aside className="hidden w-[350px] border-l bg-background md:block overflow-y-auto p-4">{rightPanel}</aside>        <Sheet open={isMobilePanelOpen} onOpenChange={setMobilePanelOpen}>          <SheetContent side="right" className="w-[90%] sm:w-[400px] pt-10">{rightPanel}</SheetContent>        </Sheet>      </div>    </div>  );}